# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod1
set $term st
# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.

font pango:JetBrainsMono Nerd Font 12px

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3
exec --no-startup-id slock

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
# exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork
# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

#Set Desktop Wallpaper
exec_always --no-startup-id feh --bg-fill --randomize wallpapers/
bindsym $mod + F6 exec feh --bg-fill --randomize wallpapers/


# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pamixer -i  5 --allow-boost && pkill -RTMIN+10 i3blocks
bindsym XF86AudioLowerVolume exec --no-startup-id pamixer -d  5 --allow-boost && pkill -RTMIN+10 i3blocks
bindsym XF86AudioMute exec --no-startup-id pamixer -t && pkill -RTMIN+10 i3blocks
#bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec $term

# kill focused window
bindsym $mod+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec --no-startup-id "j4-dmenu-desktop"
# A more modern dmenu replacement is rofi:
# bindcode $mod+40 exec "rofi -modi drun,run -show drun"
# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
# bindsym $mod+Left focus left
# bindsym $mod+Down focus down
# bindsym $mod+Up focus up
# bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+shift+n split h

# split in vertical orientation
bindsym $mod+shift+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym mod4+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
# bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)

# Translucent
# bar {
#   i3bar_command i3bar -t
#   status_command i3blocks -c ~/.config/i3blocks/config
#   colors {
#     background         #18182599
#     statusline         #cdd6f4
#     focused_statusline #cdd6f4
#     active_workspace   #181825 #cdd6f4 #89b4fa
#     focused_separator  #181825 
#     focused_workspace  #18182599 #18182599 #f9e2af
#     active_workspace   #18182599 #18182599 #89b4fa 
#     inactive_workspace #18182599 #18182599 #585b70 
#     urgent_workspace   #f38ba8 #f38ba8 #181825
#     binding_mode       #1e1e2e #1e1e2e #74c7ec 
#   }
# }

# Opaque
bar {
  status_command i3blocks -c ~/.config/i3blocks/config
  colors {
    background         #181825
    statusline         #cdd6f4
    focused_statusline #cdd6f4
    active_workspace   #181825 #cdd6f4 #89b4fa
    focused_separator  #181825 
    focused_workspace  #181825 #181825 #f9e2af
    active_workspace   #181825 #181825 #89b4fa 
    inactive_workspace #181825 #181825 #585b70 
    urgent_workspace   #f38ba8 #f38ba8 #181825
    binding_mode       #1e1e2e #1e1e2e #74c7ec 
  }
}

# for making variables, do set $name "value"
#For enabling borders, change default_border pixel value above, then uncomment below lines for colors 
# class                 border  bground text    indicator child_border
client.focused          #6272A4 #6272A4 #F8F8F2 #6272A4   #6272A4
client.focused_inactive #44475A #44475A #F8F8F2 #44475A   #44475A
client.unfocused        #282A36 #282A36 #BFBFBF #282A36   #282A36
client.urgent           #44475A #FF5555 #F8F8F2 #FF5555   #FF5555
client.placeholder      #282A36 #282A36 #F8F8F2 #282A36   #282A36

client.background       #F8F8F2

# Setting gaps
gaps inner 4
gaps outer -4

# Disable focusing new windows
# no_focus [window_role="browser"]
focus_on_window_activation urgent

#Configuring borders
smart_borders on
default_border pixel 1
# for_window [floating] border pixel 1
# for_window [tiling] border pixel 0

# Set any keybinding:
# bindsym $mod+shift+x exec i3lock --color=#111111 --show-failed-attempts --nofork
bindsym --release $mod+shift+x exec --no-startup-id slock
bindsym $mod+Tab workspace back_and_forth
bindsym $mod+Shift+P exec killall picom 

bindsym $mod+Shift+d exec rofi -show run
bindsym Ctrl+Shift+Escape exec xfce4-taskmanager
bindsym $mod+Shift+W sticky toggle
# read 1 character and mark the current window with this character
bindsym $mod+m exec i3-input -F 'mark %s' -l 1 -P 'Mark: '
# read 1 character and go to the window with the character
bindsym $mod+g exec i3-input -F '[con_mark="%s"] focus' -l 1 -P 'Goto: '

# summon greenclip for clipboard history
bindsym $mod+Shift+G exec rofi -modi "clipboard:greenclip print" -show clipboard -run-command '{cmd}'

#move workspace to another monitor
bindsym $mod+greater move workspace to output next

#dropdown/scratchpad terminal
bindsym $mod+space exec ~/scripts/nscratch.sh 1
bindsym Mod4+$mod+space exec ~/scripts/nscratch.sh 2
# bindsym $mod+3 exec ~/scripts/nscratch.sh 3

#close all notifs
bindsym Ctrl+Shift+space exec dunstctl close-all

bindsym $mod+Shift+t exec --no-startup-id xinput --list-props 14 | grep 'Device Enabled' | cut -f2 -d ':' | xargs -I {} bash -c 'if [ {} -eq 0 ]; then xinput --enable 14; else xinput --disable 14; fi'

#set a window to full opacity always
bindsym $mod+Shift+o exec picom-trans -c -o 100
#normal opacity
bindsym $mod+t exec picom-trans -c -t

#fix airpods
bindsym Mod4+b exec ~/scripts/blu f
#swap workspaces between screens
# bindsym $mod+x exec ~/swap.sh

#swap active screen
# bindsym mod1+space exec ~/scrn.sh

#Duplicate/Un-duplicate displays
# bindsym $mod+Shift+d exec ~/dupl.sh




# Screenshots
# mkdir ~/Pictures first
bindsym Print exec --no-startup-id maim | xclip -selection clipboard -t image/png && notify-send -t 1000 "SS in Clipboard" 
bindsym $mod+Shift+S exec --no-startup-id maim --select --hidecursor | xclip -selection clipboard -t image/png && notify-send -t 1000 "SS in Clipboard" 
bindsym Mod4+Shift+S exec --no-startup-id maim --select --hidecursor "/home/$USER/Pictures/$(date "+%d_%b_%r").png" && notify-send -t 1000 "SS Saved" 
bindsym Mod4+Print exec --no-startup-id maim "/home/$USER/Pictures/$(date "+%d_%b_%r").png" && notify-send -t 1000 "SS Saved"
bindsym $mod+n exec --no-startup-id maim --hidecursor -i $(xdotool getactivewindow) | xclip -selection clipboard -t image/png && notify-send -t 1000 "Window SS in Clipboard"
bindsym Mod4+n exec --no-startup-id maim --hidecursor -i $(xdotool getactivewindow) "/home/$USER/Pictures/$(date '+%H_%M_%S').png" && notify-send -t 1000 "Window SS Saved"

bindsym $mod+Shift+Print exec --no-startup-id maim --select --hidecursor "/home/$USER/Pictures/$(date "+%d_%b_%r").png"
bindsym $mod+o exec maim -s | tesseract stdin - | xclip -in -selection clipboard && notify-send "Copied Text" "$(xclip -out -selection clipboard | tr '\n' ' ')"
# Mode for launching frequently used configs
bindsym $mod+c mode "(c)onfig (i)3config (n)vim (l)inuxbkup i3(b)locks (s)cripts bash(r)c (o)ldfiles"
mode "(c)onfig (i)3config (n)vim (l)inuxbkup i3(b)locks (s)cripts bash(r)c (o)ldfiles"{
    bindsym c exec "sh -c 'cd ~/.config; st -e nvim . +Telescope\ find_files '"; mode "default"
    bindsym i exec $term -e nvim ~/.config/i3/config; mode "default"
    bindsym o exec $term -e nvim +Telescope\ oldfiles; mode "default"
    bindsym r exec $term -e nvim ~/.bashrc; mode "default"
    bindsym n exec "sh -c 'cd ~/.config/nvim/; $term -e nvim . +Telescope\ find_files '"; mode "default"
    bindsym l exec "sh -c 'cd /media/data/Linux_Backup/;$term -e nvim . +Telescope\ find_files'"; mode "default"
    bindsym b exec $term -e nvim ~/.config/i3blocks/config; mode "default"
    bindsym s exec "sh -c 'cd ~/scripts;$term -e nvim . +Telescope\ find_files'"; mode "default"
    bindsym $mod+c mode "default"
    bindsym Escape mode "default"
}
# Mode for exiting
bindsym $mod+Shift+e mode "(S)hutdown, (L)ogout, (R)eboot, sus(P)end, (H)ibernate"
mode "(S)hutdown, (L)ogout, (R)eboot, sus(P)end, (H)ibernate" {
        bindsym $mod+s exec systemctl poweroff
        bindsym $mod+l exit
        bindsym $mod+r exec systemctl reboot
        bindsym $mod+p exec systemctl suspend
        bindsym $mod+h exec systemctl hibernate

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

# Media player controls
bindsym XF86AudioPlay exec playerctl -a play-pause
bindsym XF86AudioNext exec playerctl -a next
bindsym XF86AudioPrev exec playerctl -a previous

# Brightenss controls
bindsym XF86MonBrightnessUp exec "xbacklight -inc 2 ; notify-send --hint=string:x-dunst-stack-tag:brtns -t 2000 Brightness $(xbacklight -get)"
bindsym XF86MonBrightnessDown exec "xbacklight -dec 2; notify-send --hint=string:x-dunst-stack-tag:brtns -t 2000 Brightness $(xbacklight -get)"

#Default floating modes
for_window [window_role="pop-up"] floating enable
for_window [window_role="bubble"] floating enable 
for_window [window_role="task_dialog"] floating enable
for_window [window_role="Preferences"] floating enable
for_window [window_type="dialog"] floating enable 
for_window [window_type="menu"] floating enable
# for_window [class="Brave-browser" instance="crx_jfdpbekhjmgdkmamcgbaipfbpggchgcm"] floating disable

#autostart commands
exec --no-startup-id xrdb ~/.Xresources
exec --no-startup-id source ~/.bashrc

# exec --no-startup-id setxkbmap -option caps:escape_shifted_capslock
# exec xmodmap -e "keycode 64 = Mode_switch"
# exec xmodmap -e "keysym d = d D Up"
# exec xmodmap -e "keysym f = f F Down"
# exec xmodmap -e "keysym g = g G Right"
# exec xmodmap -e "keysym s = s S Left"
exec --no-startup-id picom
# exec --no-startup-id /usr/lib/xfce4/notifyd/xfce4-notifyd
exec --no-startup-id dunst
# exec --no-startup-id python3 ~/batpop.py
# exec --no-startup-id blueman-applet
# exec --no-startup-id i3lock --color=#111111 --show-failed-attempts --nofork
exec --no-startup-id greenclip daemon>/dev/null
exec_always --no-startup-id "sh -c 'sleep 1; exec pkill -RTMIN+10 i3blocks'"

#Autostarting
exec --no-startup-id thorium-browser
# exec nvim ~/.config/nvim/init.lua +q
# exec sleep 15;~/scripts/nscratch.sh 1 && ~/scripts/nscratch.sh 1 ;sleep 2;tmux send-keys -t drop1 "nvim ~/notes/todo.md -c 'norm j<C-d>' -c 'Telescope oldfiles' -c 'q" Enter
exec sleep 20;~/scripts/nscratch.sh 1 && ~/scripts/nscratch.sh 1 ;sleep 2;tmux send-keys -t drop1 "nvim ~/notes/todo.md +'norm 5j2k' +'Telescope oldfiles'" Enter && sleep 35;tmux send-keys -t drop1 "h" Escape "h" Escape ":q" Enter "clear" Enter && sleep 3;tmux send-keys -t drop1 "clear" Enter
# exec --no-startup-id i3-msg 'workspace 2; exec /usr/bin/firefox; workspace 1'
# exec --no-startup-id easyeffects --gapplication-service
exec --no-startup-id blugon
# exec ~/edge.sh #edge resistance
#set repeat delay to 200ms at rate 55
exec xset r rate 220 42
# exec $term -e nvim ~/.config/nvim/init.lua +q 
